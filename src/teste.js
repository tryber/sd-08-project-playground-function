
let nome = ['Lucas', 'Cassiano', 'Ferraz', 'Paolillo'];
let pNome, uNome, cont;
  // for (let index = 0; index < nome.length - 1; index += 1) {
  //   if (nome[index] === nome[0]) {
  //     pNome = nome[index];
  //   }
  //   if(nomde[index] === nome.length-1){
  //     uNome = 
  //   }
  pNome = nome[0];
  uNome = nome[nome.length - 1];
  cont = uNome + pNome;

  console.log(cont)

